Tasks:
-تو همه بخش ها ارور رو به رو رو داریم => error undefined key که احتمالا سر async نبودنه چون بار دوم یا سوم رفع میشه
-سایت از لحاظ لودینگ طول میکشه که حدس میزنم احتمالا از سمت سرور سرعتش پایینه
-زمانی که route های یک سرور رو میگیری یک route تستی هست که باید دیلیت شه root@{name of server}
-تو بخش user management  یک بخش هست access اون باید دیلیت شه و دسترسی ادمین باید مطلق باشه گزینه ها باید در صورت ادمین بودن disable بشن
-یک فیلد باید اضافه کنیم تو بخش add modules که اسمش هست unique name در صورتی که کاربر یک فایل رو گذاشت باید اسم فیلد module name بشه اسم فایل بدون پسوند 
-server setting => setting => modules =>export=> باید async await باشه تا صب کنه از سرور جواب بیاد بعد خروجی بده نه اینکه یک فایل بده به اسم name.yaml
-حل مشکل update config که نباید در فایل yaml استرینگ بده چون موجب ارور میشه باید parse بشه 
یک ازوز تو اخرین بخش هست که اسمش the payload is invalid هست نباید اونجا باشه

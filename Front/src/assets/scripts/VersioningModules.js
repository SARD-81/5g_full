
let Init = []
function VersioningModule(version, comment) {
    let Version = version;
    let Comment = comment;
    Init.push({ ModuleVersion: `${Version}`, ModuleComment: `${comment}`, Module: ``, content })
    console.log(Init)
}
export default VersioningModule;